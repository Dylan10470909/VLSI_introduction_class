`timescale 1ns/1ns
module Counter_TB();

reg	 clk;
reg	 en;
reg     rst;
reg     slt;
reg     [3:0]out;
Counter dut(clk,en,rst,slt,out);
     
     always #5 begin 
         clk = ~clk; 
      end 

initial begin 
 clk =0;
 rst=1;
 en=0;
 slt=0;
 #10
 rst=0;
 en=0;

 slt=0;
 #10
 rst=1;
 en = 1;
 slt=0;
 #10
 en = 1;
 slt=1;
  #50
$finish;
end 

initial begin
//請同學改成自己的路徑
	$fsdbDumpfile("/home/110_course/IIC/C108110249/Desktop/Lab07/Counter_TB.fsdb");
	$fsdbDumpvars(0,Counter_TB);
end

endmodule 
